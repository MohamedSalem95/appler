<div class="row">

    <div class="col-md-7"> 

        <h5 class="fw-bold"> مواعيدي اليوم </h5>
        <% appointments = current_user.appointments.for_today %>
        <% if appointments.any? %>


            <table class="table table-borderless table-bordered table-hover table-sm">
                <thead class="bg-prime text-white">
                    <th></th>
                    <th> الاسم </th>
                    <th> الوقت </th>
                    <th> سبب المقابله </th>
                    <th> الحاله </th>
                    <td></td>
                </thead>

                <tbody>
                    <% counter = 0 %>
                    <% appointments.each do |appointment| %>
                        <tr class="bg-light">
                            <td> <b> <%= counter += 1 %> </b> </td>
                            <td> <b> <%=  appointment.name %> </b> </td>
                            <td> <b> <%=  appointment.app_time.strftime('%I:%M %p') %> </b> </td>
                            <td>
                                <b>
                                    <% if appointment.description.present? %>
                                        <%=  appointment.description.truncate(50) %>
                                    <% else %>
                                        <span> لا يوجد تفاصيل. </span>
                                    <% end %>
                                </b> 
                            </td>
                            <td>
                            <Status :id=<%= appointment.id %> :status=<%= appointment.status %>></Status>
                            <!--
                                <%# if appointment.status == 1 %>
                                    <span class="badge bg-info"> لم يدخل بعد </span>
                                <%# end %>

                                <%# if appointment.status == 2 %>
                                    <span class="badge bg-primary"> تم الموافقه </span>
                                <%# end %>

                                <%# if appointment.status == 3 and appointment.delay_date != Date.today %>
                                    <span class="badge bg-warning"> تم التاجيل </span>
                                <%# end %>

                                <%# if appointment.status == 3 and appointment.delay_date == Date.today %>
                                    <span class="badge bg-info"> لم يدخل بعد (مؤجل) </span>
                                <%# end %>

                                <%# if appointment.status == 4 %>
                                    <span class="badge bg-danger"> تم الرفض </span>
                                <%# end %>

                                <%# if appointment.status == 5 %>
                                    <span class="badge bg-success"> تم الدخول </span>
                                <%# end %>
                                -->
                            </td>

                            <td>
                            </td>
                        </tr>
                    <% end %>
                </tbody>

                <tfoot>
                   <tr class="bg-prime-var2 text-white fw-bold"> 
                        <td colspan=6>
                            عدد المواعيد اليوم 
                            <%= appointments.count %>
                            موعد 
                        </td>
                   </tr>
                </tfoot>
            </table>


            


        <% else %>

            <p class="fw-bold"> لا يوجد مواعيد لليوم. </p>

        <% end %>

        <hr/>
        <h5 class="fw-bold"> جميع المواعيد </h5>
        <% all = current_user.appointments.latest %>
        <% if all.any? %>


            <table class="table table-borderless table-bordered table-hover table-sm">
                <thead class="bg-prime text-white">
                    <th></th>
                    <th> الاسم </th>
                    <th> الوقت </th>
                    <th> سبب المقابله </th>
                    <th> الحاله </th>
                    <td></td>
                </thead>

                <tbody>
                    <% counter = 0 %>
                    <% all.each do |appointment| %>
                        <tr class="bg-light">
                            <td> <b> <%= counter += 1 %> </b> </td>
                            <td> <b> <%=  appointment.name %> </b> </td>
                            <td> <b> <%=  appointment.app_time.strftime('%I:%M %p') %> </b> </td>
                            <td>
                                <b>
                                    <% if appointment.description.present? %>
                                        <%=  appointment.description.truncate(50) %>
                                    <% else %>
                                        <span> لا يوجد تفاصيل. </span>
                                    <% end %>
                                </b> 
                            </td>
                            <td>
                            <Status :id=<%= appointment.id %> :status=<%= appointment.status %>></Status>
                            <!--
                                <%# if appointment.status == 1 %>
                                    <span class="badge bg-info"> لم يدخل بعد </span>
                                <%# end %>

                                <%# if appointment.status == 2 %>
                                    <span class="badge bg-primary"> تم الموافقه </span>
                                <%# end %>

                                <%# if appointment.status == 3 and appointment.delay_date != Date.today %>
                                    <span class="badge bg-warning"> تم التاجيل </span>
                                <%# end %>

                                <%# if appointment.status == 3 and appointment.delay_date == Date.today %>
                                    <span class="badge bg-info"> لم يدخل بعد (مؤجل) </span>
                                <%# end %>

                                <%# if appointment.status == 4 %>
                                    <span class="badge bg-danger"> تم الرفض </span>
                                <%# end %>

                                <%# if appointment.status == 5 %>
                                    <span class="badge bg-success"> تم الدخول </span>
                                <%# end %>
                                -->
                            </td>

                            <td>
                            </td>
                        </tr>
                    <% end %>
                </tbody>

                <tfoot>
                   <tr class="bg-prime-var2 text-white fw-bold"> 
                        <td colspan=6>
                            عدد المواعيد  
                            <%= all.count %>
                            موعد 
                        </td>
                   </tr>
                </tfoot>
            </table>


            


        <% else %>

            <p class="fw-bold"> لا يوجد مواعيد. </p>

        <% end %>

     </div>

</div>